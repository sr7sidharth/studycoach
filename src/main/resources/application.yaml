spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studycoach
    username: studyuser
    password: studypass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080